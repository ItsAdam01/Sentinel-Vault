<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Vault</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#0f0f10] text-gray-300 min-h-screen flex">

    <!-- SIDEBAR -->
    <aside class="w-64 bg-[#141416] hidden md:flex flex-col h-screen sticky top-0 border-r border-black/40">

    <div class="flex-1 flex flex-col overflow-y-auto">
        <div class="px-6 py-6 border-b border-black/40">
            <h2 class="text-lg font-semibold text-white">Vault</h2>
        </div>

        <nav class="px-4 py-6 space-y-1 text-sm">
            <a href="/vault" class="block px-4 py-2 rounded-md bg-[#1c1d20] text-white font-medium">
                Dashboard
            </a>
            <a href="/audit" class="block px-4 py-2 rounded-md text-gray-400 hover:bg-[#1c1d20] hover:text-white transition">
                Security Logs
            </a>
        </nav>
    </div>

    <div class="px-6 py-6 text-xs text-gray-500 border-t border-black/40 bg-[#141416]">
        <p>Logged in as</p>
        <div class="text-gray-300 font-medium mt-1 mb-4">
            <%= user.username %>
        </div>

        <a href="/logout"
           class="block w-full text-center bg-red-600 px-4 py-2 rounded text-white hover:bg-red-500 transition text-sm font-medium">
            Logout
        </a>
    </div>

</aside>



    <!-- MAIN CONTENT -->
    <div class="flex-1 flex flex-col">

        <!-- Page Header -->
        <header class="px-10 py-8">
            <h1 class="text-3xl font-semibold text-white tracking-tight">
                Your Vault
            </h1>
            <p class="text-gray-500 text-sm mt-2">
                Manage your encrypted vault secrets below.
            </p>
        </header>


        <!-- Content -->
        <main class="px-10 pb-12">

            <div class="max-w-3xl bg-[#17181a] rounded-lg p-10 shadow-xl shadow-black/40">

                <!-- Profile Section -->
                <div class="flex items-center gap-6 mb-10 pb-10 border-b border-black/40">
                    
                    <img src="<%= user.avatar_url %>" 
                         alt="Avatar" 
                         class="w-20 h-20 rounded-full shadow-lg shadow-black/50">

                    <div>
                        <h2 class="text-2xl font-semibold text-white">
                            Welcome, <%= user.username %>
                        </h2>
                        <p class="text-gray-500 mt-2">
                            Your personal encrypted storage space.
                        </p>
                    </div>
                </div>


                <% if (typeof message !== 'undefined') { %>
                    <div class="mb-8 px-5 py-4 bg-green-600 text-white rounded-md text-sm shadow-md shadow-black/30">
                        <%= message %>
                    </div>
                <% } %>


                <!-- Form -->
                <form action="/vault/save" method="POST" class="space-y-6">

                    <div>
                        <label class="block text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">
                            Your Encrypted Secret
                        </label>

                        <textarea 
                            name="secret" 
                            rows="6" 
                            class="w-full bg-[#111112] border border-black/40 rounded-md p-4 text-gray-200 focus:outline-none focus:ring-1 focus:ring-gray-500 font-mono text-sm resize-none"
                            placeholder="Enter your secret here..."
                        ><%= user.secret || '' %></textarea>
                    </div>
                    
                    <button type="submit" 
                            class="bg-blue-600 hover:bg-blue-500 px-8 py-3 rounded-md font-semibold text-white transition shadow-md shadow-black/30">
                        💾 Save Changes
                    </button>

                </form>

            </div>

        </main>
    </div>

</body>
</html>
