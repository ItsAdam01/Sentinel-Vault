HELLO WORLD
<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Vault</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-900 text-white min-h-screen">
    <nav class="border-b border-slate-800 bg-slate-900/50 backdrop-blur-md sticky top-0 z-10">
        <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
            <div class="flex items-center gap-2 font-bold text-xl">
                <span class="text-blue-500">🛡️</span> Sentinel
            </div>
            <div class="flex items-center gap-6">
                <a href="/audit" class="text-slate-400 hover:text-white transition-colors">Audit Logs</a>
                <a href="/logout" class="bg-red-500/10 text-red-500 px-4 py-1.5 rounded-full hover:bg-red-500 hover:text-white transition-all text-sm font-medium">Logout</a>
            </div>
        </div>
    </nav>

    <main class="max-w-4xl mx-auto p-6 mt-8">
        <div class="bg-slate-800 rounded-2xl p-8 border border-slate-700 shadow-xl">
            <div class="flex items-center gap-6 mb-8 border-b border-slate-700 pb-8">
                <img src="<%= user.avatar_url %>" alt="Avatar" class="w-20 h-20 rounded-full border-4 border-blue-500/20 shadow-lg">
                <div>
                    <h1 class="text-3xl font-bold">Welcome, <%= user.username %></h1>
                    <p class="text-slate-400">Manage your encrypted vault secrets below.</p>
                </div>
            </div>

            <% if (typeof message !== 'undefined') { %>
                <div class="mb-6 p-4 bg-emerald-500/10 border border-emerald-500/20 text-emerald-400 rounded-xl flex items-center gap-2">
                    <span class="text-xl">✅</span> <%= message %>
                </div>
            <% } %>

            <form action="/vault/save" method="POST" class="space-y-4">
                <label class="block text-sm font-medium text-slate-400 mb-2 uppercase tracking-wider">Your Encrypted Secret</label>
                <textarea 
                    name="secret" 
                    rows="6" 
                    class="w-full bg-slate-900 border border-slate-700 rounded-xl p-4 text-slate-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all outline-none font-mono"
                    placeholder="Enter your secret here..."
                ><%= user.secret || '' %></textarea>
                
                <button type="submit" class="bg-blue-600 hover:bg-blue-500 px-8 py-3 rounded-xl font-bold transition-all flex items-center gap-2">
                    <span>💾</span> Save Changes
                </button>
            </form>
        </div>
    </main>
</body>
</html> -->